/* ===================================
   Progress — Console-Integrated Progress Bar
   (No more floating widget — progress is now inside the console)
   =================================== */

// The progress bar is now inside the console section.
// No drag, minimize, or close logic needed.
// All progress updates are handled by transfer.js via update_progress().
